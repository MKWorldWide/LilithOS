# Project Low-Key Bootloader

This folder will contain minimal ARM payloads for chainloading a custom OS (e.g., Linux).
- Place your bootloader source and build scripts here.
- Output: build/bootloader.bin